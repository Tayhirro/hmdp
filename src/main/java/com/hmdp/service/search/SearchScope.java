package com.hmdp.service.search;

/**
 * 统一搜索支持的业务检索域。
 *
 * 这里列的是“允许被搜索的内容类型”，不是数据库表清单。所谓任意搜索，是在已经注册的检索域中
 * 统一理解和分发用户 Query，不代表可以绕过业务边界搜索任意表和内部字段。
 */
public enum SearchScope {

    /** 店铺名称、类型、地址等本地生活供给。 */
    SHOP,

    /** 探店博客的标题和正文。 */
    BLOG,

    /** 可公开检索的用户昵称与主页摘要。 */
    USER
}
