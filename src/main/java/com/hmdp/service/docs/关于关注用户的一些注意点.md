# 关于关注用户的一些注意点

## 1. 高并发下单行 JSON 更新冲突风险

如果把“某个用户关注了谁”全部存进同一行 JSON（例如 `user_id` 对应一个 `follows` 数组），在大量关注/取关并发发生时，会出现明显的写冲突风险：

1. 同一个用户的关注变更都落在同一行，形成热点行。
2. 多事务并发更新该行时，容易出现行锁竞争、等待时间变长，吞吐下降。
3. 在高峰期可能引发锁等待超时，导致接口响应抖动。

这也是关注关系通常拆成关系表（`user_id` + `follow_user_id` 一行一关系）的重要原因之一。
